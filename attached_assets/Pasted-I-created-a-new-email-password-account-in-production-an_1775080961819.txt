I created a new email/password account in production and then used forgot password for that same account email, but I still did not receive any email.

This is now a real production issue.

Do a concrete production diagnosis for THIS exact account and latest forgot-password attempt.

Do not guess. Inspect actual production logs, database state, and email-send behavior.

I need the following:

## 1. User/account confirmation
For the exact email I just created:
- confirm the user exists in production
- confirm passwordHash is present
- confirm this is an email/password account, not Google-only
- show createdAt / updatedAt

## 2. Forgot-password request evidence
For the latest attempt with that email:
- did POST /api/auth/forgot-password hit production backend?
- timestamp
- request ID if available
- response status
- audit events written

## 3. Token creation evidence
- was a password_reset_tokens row created for this user?
- show:
  - createdAt
  - expiresAt
  - usedAt
- if not created, explain exactly why not

## 4. Email send evidence
- was sendPasswordResetEmail called?
- was sendViaGmail called?
- did Gmail/API accept or reject the send?
- exact provider error if any
- sender identity used
- message ID if send succeeded

## 5. Rate limiting evidence
- was this request blocked by per-IP or per-email throttling?
- if yes, show the exact evidence
- if no, confirm that clearly

## 6. Delivery root cause
Give one clear root cause:
- app logic bug
- token not created
- Gmail send failure
- sender permissions problem
- sender identity mismatch
- rate limiting
- downstream delivery issue
- something else

## 7. Fix it now if broken
If the issue is in code or config:
- fix it now
- show the exact changes
- explain what to retest

## 8. Output format
1. Account confirmation
2. Forgot-password request evidence
3. Token evidence
4. Email send evidence
5. Rate limiting evidence
6. Root cause
7. Fix applied
8. How I should retest

Important:
This is production, so be precise.
Do not give me generic reassurance.