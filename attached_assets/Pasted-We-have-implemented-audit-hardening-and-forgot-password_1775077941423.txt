We have implemented audit hardening and forgot-password flow.

Now we need to CLOSE THE REMAINING PRODUCTION GAPS.

Do not summarize.
Do not explain theory.
Make code changes and prove them.

--------------------------------------------------

## 1. Enforce audit at the system level (no bypass possible)

Right now audit enforcement is soft.

Implement ONE of the following (or equivalent):

OPTION A (preferred):
- create a wrapped database access layer
- block direct db.insert / update / delete usage
- enforce all mutations go through a controlled interface that requires audit context

OPTION B:
- runtime guard that throws if a mutation occurs outside executeWithAudit
- stack trace or context detection required

Then:
- prove no mutation can execute without audit

--------------------------------------------------

## 2. Fail-safe for critical audit failure

Update executeWithAudit:

Define a list of CRITICAL actions:
- password reset
- conversation creation
- showing requests
- offer actions

If audit fails AFTER retries for critical actions:
- either:
  - abort the action
  - OR mark the action as incomplete / unsafe
- log a CRITICAL system error

Show exact behavior implemented.

--------------------------------------------------

## 3. Add real rate limiting to forgot-password

Implement:

- per-IP rate limit (example: 5 requests per 10 minutes)
- per-email rate limit (example: 3 per hour)

Store:
- timestamp
- attempt count

If limit exceeded:
- still return generic response
- do NOT send email
- log audit event: forgot_password_rate_limited

--------------------------------------------------

## 4. Invalidate sessions on password reset

When password is successfully reset:

- invalidate ALL existing sessions for that user

Depending on architecture:
- delete session records
- rotate session secret
- mark tokens invalid

Show exact implementation.

--------------------------------------------------

## 5. Strengthen password policy

Implement and enforce:

- minimum 8–12 characters
- at least 1 number
- at least 1 letter
- optional: symbol requirement

Reject weak passwords with clear message.

--------------------------------------------------

## 6. Enforce audit integrity in CI

Modify:

scripts/verify-audit-integrity.ts

So that:
- it exits with non-zero status on failure

This should:
- fail builds
- fail deployments

--------------------------------------------------

## 7. Prove everything works

Run and show:

- find-unaudited-mutations.ts output (should be clean or justified)
- verify-audit-integrity.ts output (PASS)
- forgot-password rate limiting behavior
- session invalidation behavior

--------------------------------------------------

## OUTPUT FORMAT

1. What was enforced
2. What was blocked/prevented
3. Files changed
4. Tests added
5. Scripts output
6. Security improvements achieved
7. Remaining risks (if any)

--------------------------------------------------