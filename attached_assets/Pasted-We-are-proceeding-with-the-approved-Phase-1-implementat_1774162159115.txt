We are proceeding with the approved Phase 1 implementation for the core loop (buyer_interest, conversations, messaging, showing requests).

Before building, I want you to refine the implementation plan and schema with the following REQUIRED improvements.

These are not new features — they are structural and UX-critical enhancements that must be included in Phase 1 to avoid future refactors.

---

## 1. Conversation Model Enhancements

Update the conversations table to include:

- lastMessageAt (timestamp)
  Purpose:
  - sorting conversation lists
  - displaying most recent activity
  - powering notification logic

- initiatedBy (enum: 'buyer', 'agent', 'system')
  Purpose:
  - analytics (who starts conversations)
  - UX personalization
  - future monetization insights

Make sure:
- lastMessageAt updates on every new message
- existing queries sort by lastMessageAt DESC

---

## 2. Buyer Interest Model Enhancements

Ensure buyer_interest includes:

- lastActivityAt (timestamp)
  This MUST update on:
  - new message in associated conversation
  - showing request created/updated
  - offer sent/responded
  - stage changes

- conversationId (nullable FK)
  Purpose:
  - link interest to conversation once engagement begins
  - maintain separation between interest and messaging layers

- initiatedBy (optional but preferred)
  (who triggered the interest: buyer swipe, agent outreach, system match)

---

## 3. Conversation ↔ Interest Linking Logic

Define clear rules:

- buyer_interest is created or updated on:
  - swipe right
  - request showing
  - request info
  - ask question

- conversation is ONLY created when:
  - a message is sent
  - showing request is submitted
  - request info is submitted
  - reverse offer is sent

- when a conversation is created:
  - attach conversationId to the corresponding buyer_interest record
  - update stage to "conversation_started"

---

## 4. Buyer UX: Agent Response Visibility

In the Buyer Dashboard:

For each property with interest:
- show a status indicator:
  - "No response yet"
  - "Agent responded"
  - "Conversation active"

Implementation:
- derive from:
  - presence of conversation
  - last message sender
  - timestamps

---

## 5. Buyer Nudge System (Lightweight V1)

If:
- buyer_interest exists
- AND no conversation exists
- AND no agent response after X hours (configurable, e.g., 12–24h)

Then:
- show UI prompt in dashboard or property view:
  "Want to message the agent?"

Do NOT implement automation or messaging triggers.
Only UI-based nudges.

---

## 6. Messaging System Guardrails

Ensure:
- no automatic conversation creation on swipe
- conversations are always tied to real actions
- message types remain limited to:
  - text
  - showing_request
  - reverse_offer
  - system

Do NOT expand message types in Phase 1.

---

## 7. Notification Logic Refinement

- In-app notifications are primary
- Email is secondary

Ensure:
- message_received emails are rate-limited (already planned)
- all notifications deep link to:
  - /conversations/:id
  - OR specific showing detail views

Avoid generic dashboard links.

---

## 8. Sorting & Ranking Foundations

Ensure both:

### Conversations:
- sorted by lastMessageAt DESC

### Buyer Interest (Leads tab):
- sorted by lastActivityAt DESC

These fields will later power prioritization, monetization, and agent workflows.

---

## 9. Do NOT Overbuild

Reconfirm:
- no read receipts
- no websocket / realtime yet (polling only)
- no kanban pipeline yet
- no advanced negotiation system
- no payment system

Keep Phase 1 focused on:
interest → engagement → response → showing

---

## Deliverables

1. Updated schema changes
2. Updated API design (if needed)
3. Updated frontend component behavior
4. Clear description of how data flows:
   swipe → interest → conversation → showing → response
5. Then begin implementation

Important:
- Reuse existing architecture patterns
- Do not break current functionality
- Keep changes minimal but high quality
- Be explicit about files being modified