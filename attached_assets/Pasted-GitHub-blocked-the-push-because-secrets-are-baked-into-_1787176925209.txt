GitHub blocked the push because secrets are baked into the commit history. Fix this properly:

Step 1 — Move Mapbox token to environment variable

Find the hardcoded Mapbox token in these files and replace it with an environment variable:

client/src/components/MapView.tsx line 7
client/src/pages/AgentDashboard.tsx line 188

Replace the hardcoded token string with:

typescript
const MAPBOX_TOKEN = import.meta.env.VITE_MAPBOX_TOKEN;

Then add VITE_MAPBOX_TOKEN to Replit Secrets with the actual token value.

Step 2 — Check .replit for the exposed GitHub token

Open .replit line 25. If a GitHub personal access token is hardcoded there, remove it and add it to Replit Secrets as GITHUB_TOKEN instead.

Step 3 — Add a .gitignore entry

Make sure .replit is in .gitignore so it never gets committed again:

bash
echo ".replit" >> .gitignore
git add .gitignore

Step 4 — Rewrite git history to remove the secrets

Use git filter-branch or BFG to scrub the secrets from all historical commits:

bash
# Install BFG
wget https://repo1.maven.org/maven2/com/madgag/bfg/1.14.0/bfg-1.14.0.jar -O /tmp/bfg.jar

# Create a file with the secrets to remove (replace ACTUAL_TOKEN with the real values)
echo "ACTUAL_MAPBOX_TOKEN_HERE" > /tmp/secrets.txt
echo "ACTUAL_GITHUB_TOKEN_HERE" >> /tmp/secrets.txt

# Run BFG to scrub history
java -jar /tmp/bfg.jar --replace-text /tmp/secrets.txt .

# Clean up
git reflog expire --expire=now --all
git gc --prune=now --aggressive

Step 5 — Verify secrets are gone

bash
git log --all --full-history -- "client/src/components/MapView.tsx" | head -5
grep -r "sk\.eyJ" . --include="*.ts" --include="*.tsx" --include="*.js" | grep -v node_modules | grep -v .git
grep -r "ghp_\|github_pat_" . --include="*.ts" --include="*.tsx" --include="*.replit" | grep -v node_modules | grep -v .git

Both grep commands must return empty.

Step 6 — Push again

bash
git remote set-url github https://listedbydavid:${GITHUB_TOKEN}@github.com/listedbydavid/xucasa.git
git push github main:main --force 2>&1
git remote set-url github https://github.com/listedbydavid/xucasa.git

Show me the output of every step. Do not print the actual token values in your response.

Also Do This Right Now — Rotate Your Exposed Keys

While Replit works on the history rewrite, you need to invalidate the exposed tokens immediately since they're already in the failed push attempt which GitHub may have logged:

Rotate Mapbox token:

Go to mapbox.com → Account → Access Tokens
Delete the exposed token
Create a new one
Update the VITE_MAPBOX_TOKEN secret in Replit with the new value

Rotate GitHub token:

Go to github.com → Settings → Developer Settings → Personal Access Tokens
Delete the token that was exposed
Generate a new one
Update GITHUB_TOKEN in Replit Secrets

Do the rotation now — the old tokens should be considered compromised.