We are about to test the forgot-password flow in production.

Do NOT guess. Provide exact steps, URLs, and what to verify in logs and database.

--------------------------------------------------

## 1. Pre-Publish Checklist

Confirm BEFORE publish:

- Current commit is deployed-ready (no pending migrations)
- password_reset_tokens table exists in production
- audit_events table has errorMessage column
- Email provider (Gmail integration) is connected and healthy in production
- REPLIT_DEPLOYMENT_URL is set and will be used for reset links
- No dev-only guards block email sending in production

Output: PASS / FAIL for each

--------------------------------------------------

## 2. Controlled Test Account

Define a safe test account:

- email: (use david@xucasa.com or create test account)
- confirm:
  - account exists
  - has passwordHash (not Google-only)
  - not rate-limited currently

--------------------------------------------------

## 3. Exact Test Steps (Production)

Provide exact steps with URLs:

1. Go to production login page: [FULL URL]
2. Click "Forgot password?"
3. Submit email
4. Expected UI response text
5. Wait time expectation for email
6. Email subject + sender
7. Exact reset link format (must include production domain)

--------------------------------------------------

## 4. Backend Verification (MANDATORY)

After triggering forgot-password:

Show:

- password_reset_tokens row created:
  - user_id
  - expires_at
  - used_at (should be NULL)

- audit events:
  - forgot_password_requested
  - forgot_password_email_sent (or failure)

- confirm:
  - no rate limiting triggered
  - email send succeeded (no "Insufficient Permission")

--------------------------------------------------

## 5. Reset Flow Verification

After clicking email link:

- confirm token is accepted
- reset password with valid password
- confirm:
  - password updated
  - token marked used
  - token cannot be reused

Show DB evidence:
- used_at timestamp populated

--------------------------------------------------

## 6. Session Invalidation Proof

- log in BEFORE reset (create active session)
- perform reset
- verify:
  - sessions table entries for that user are deleted
  - old session requests fail (401)
  - new login works

Show:
- before/after session records

--------------------------------------------------

## 7. Rate Limiting Check

- trigger forgot-password multiple times
- confirm:
  - limit is enforced
  - no new token created after limit
  - no email sent after limit
  - audit event: forgot_password_rate_limited

--------------------------------------------------

## 8. Final Verdict

Answer clearly:

- Production forgot-password flow: PASS / FAIL
- Any issues found
- Safe for real users: YES / NO

--------------------------------------------------

## OUTPUT FORMAT

1. Pre-publish checklist
2. Test steps
3. Backend evidence
4. Reset verification
5. Session invalidation proof
6. Rate limit proof
7. Final verdict

--------------------------------------------------